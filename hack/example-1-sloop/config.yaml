apiVersion: app.slooper.io/v1 
Kind: Configurator
metadata:
  name: sloop-config
spec:
  template: default
  global:
    namespace: app
    values:
      - values.yaml
  sources:
    - name: app
      path: app/*.yaml
      namespace: app-ns
      values:
        - values.yaml 

    